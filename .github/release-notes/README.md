# Release notes

Before creating a major or minor release tag, add a Markdown file named after
the tag, for example:

```text
.github/release-notes/v0.1.0.md
```

The file is used as the custom body of the GitHub Release. GitHub also adds
its automatically generated commit list. If the file is missing, the release
still succeeds with generated notes only.
